# 短信验证码接口接入文档

> 本文档依据 `webim-vue-demo` 项目整理，用于指导其他项目完整复刻"获取短信验证码"能力。
> 覆盖三个业务场景：**登录**、**注册**、**重置密码**。

---

## 1. 整体概览

获取短信验证码分为两条链路，前置依赖不同：

| 场景 | 前置依赖 | 短信接口 |
| --- | --- | --- |
| 登录 | 阿里云验证码 2.0（滑块/拼图验证 + AES-GCM 加密） | `POST /inside/app/sms/send/v2` |
| 注册 / 重置密码 | 图片验证码（环信内置图形验证码） | `POST /inside/app/sms/send` |

两套链路共用同一域名与请求封装，但请求参数、错误处理方式不同，需分别接入。

---

## 2. 环境配置

### 2.1 服务域名（baseURL）

```js
// 线上环境（正式）
const defaultBaseUrl = '//a1.easemob.com';

// 沙箱环境（联调）
// const defaultHsbBaseUrl = '//a1-hsb.easemob.com';

// 最终请求地址：window.location.protocol + baseUrl + 接口路径
// 例：https://a1.easemob.com/inside/app/sms/send/v2
```

### 2.2 请求封装规范（axios 实例）

```js
axios.create({
  withCredentials: false,
  baseURL: `${window.location.protocol}${defaultBaseUrl}`,
  timeout: 30000,
  headers: { 'Content-Type': 'application/json' },
});
```

响应拦截器规则：

- HTTP status ≥ 400 → 视为错误，`reject(new Error(res.desc || 'Error'))`
- HTTP status 403 → 提示"您没有权限进行查询和操作!"
- 其余 → 直接返回 `response.data`

### 2.3 环境变量（阿里云验证码）

敏感配置通过环境变量注入，**不要硬编码进代码/仓库**：

```bash
# .env.local
VUE_APP_SCENE_ID=xxx   # 阿里云验证码 2.0 场景 ID
VUE_APP_PREFIX=xxx      # 阿里云验证码身份标
VUE_APP_SECRET=xxx      # AES 密钥（base64 字符串，用于加密验证参数）
```

对应代码读取方式：

```js
export const SCENE_ID = process.env.VUE_APP_SCENE_ID || '';
export const PREFIX = process.env.VUE_APP_PREFIX || '';
export const secret = process.env.VUE_APP_SECRET || '';
```

---

## 3. 接口清单

| 接口 | 方法 | 路径 | 用途 |
| --- | --- | --- | --- |
| 登录短信验证码 | POST | `/inside/app/sms/send/v2` | 登录场景发送验证码（需阿里云人机验证） |
| 获取图片验证码 | GET | `/inside/app/image` | 注册/重置密码前置，获取图形验证码 |
| 注册/重置密码短信验证码 | POST | `/inside/app/sms/send` | 注册/重置密码场景发送验证码（需图片验证码） |

---

## 4. 接口详情

### 4.1 登录短信验证码（v2，带阿里云人机验证）

**请求**

```
POST /inside/app/sms/send/v2
Content-Type: application/json
```

**请求参数（body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `phoneNumber` | string | 是 | 手机号（11 位，`/^1[3-9]\d{9}$/`） |
| `captchaVerifyParam` | string | 是 | 阿里云验证码通过后的验证参数，经 **AES-GCM 加密**后的 base64 字符串 |

**响应**

```json
{ "code": 200 }
```

`code === 200` 表示发送成功，随后启动 60 秒倒计时。

**错误响应**：HTTP 非 200 状态，错误信息在响应体字段中，按 `error_description` / `errorInfo` 判断（详见第 6 节）。

### 4.2 获取图片验证码

**请求**

```
GET /inside/app/image
```

无参数。

**响应**

```json
{
  "data": {
    "image_id": "xxx",
    "image_enabled": "true"
  }
}
```

**图片展示 URL 拼接规则**（后端返回的 `image_id` 直接拼进 URL 渲染）：

```
${window.location.protocol}//a1.easemob.com/inside/app/image/${data.image_id}
```

注意：仅当 `data.image_enabled === 'true'` 时才展示图片验证码输入框。

### 4.3 注册 / 重置密码短信验证码

**请求**

```
POST /inside/app/sms/send
Content-Type: application/json
```

**请求参数（body）**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `phoneNumber` | string | 是 | 手机号（11 位） |
| `imageId` | string | 是 | 图片验证码 ID（来自 4.2 接口返回的 `image_id`） |
| `imageCode` | string | 是 | 用户输入的图片验证码内容 |

**响应**：成功即 200（无特殊业务码，发送成功后提示"验证码已发送"）。

**错误响应**：HTTP 非 200 状态，响应体为 `{ code, errorInfo }`，按第 6 节错误码表映射提示文案。

---

## 5. 阿里云验证码 2.0 集成（登录场景必装）

### 5.1 引入 SDK

在 `public/index.html` 的 `<head>` 中引入（全局挂载 `window.initAliyunCaptcha`）：

```html
<script type="text/javascript" src="https://o.alicdn.com/captcha-frontend/aliyunCaptcha/AliyunCaptcha.js"></script>
```

### 5.2 初始化

仅在**生产环境**初始化（`process.env.NODE_ENV === 'production'`），页面挂载时调用一次：

```js
window.initAliyunCaptcha({
  SceneId: SCENE_ID,           // 环境变量
  prefix: PREFIX,              // 环境变量
  mode: 'popup',               // 弹出式验证码
  element: '#captcha-element', // 验证码渲染容器（DOM 需常驻页面，失败时隐藏）
  button: '#captcha-button',   // 触发弹窗的按钮
  captchaVerifyCallback: captchaVerifyCallback, // 验证通过回调
  onBizResultCallback: onBizResultCallback,     // 业务结果回调
  getInstance: getInstance,    // 绑定验证码实例
  slideStyle: { width: 360, height: 40 },
  language: 'cn',
});
```

组件卸载时需清理弹窗 DOM，避免重复挂载导致回调多次触发：

```js
document.getElementById('aliyunCaptcha-mask')?.remove();
document.getElementById('aliyunCaptcha-window-popup')?.remove();
```

### 5.3 完整发送流程（时序）

```
用户点击"获取验证码"按钮(#captcha-button)
  → 弹出阿里云验证码
  → 用户完成滑块/拼图验证
  → 触发 captchaVerifyCallback(captchaVerifyParam)
  → 将 captchaVerifyParam 用 AES-GCM 加密（密钥=环境变量 VUE_APP_SECRET）
  → 调用 POST /inside/app/sms/send/v2  { phoneNumber, captchaVerifyParam }
  → code === 200 → 提示成功，启动 60s 倒计时
```

### 5.4 AES-GCM 加密（captchaVerifyParam 加密）

```js
export async function encryptAES(plaintext, secretKey) {
  const encoder = new TextEncoder();
  const data = encoder.encode(plaintext);

  const binaryString = window.atob(secretKey); // 密钥为 base64
  const keyData = new Uint8Array(binaryString.length);
  for (let i = 0; i < binaryString.length; i++) {
    keyData[i] = binaryString.charCodeAt(i);
  }

  const key = await crypto.subtle.importKey(
    'raw', keyData,
    { name: 'AES-GCM', length: 256 },
    false, ['encrypt'],
  );
  const iv = crypto.getRandomValues(new Uint8Array(12));
  const encrypted = await crypto.subtle.encrypt({ name: 'AES-GCM', iv }, key, data);
  const encryptedArray = new Uint8Array(
    Array.from(iv).concat(Array.from(new Uint8Array(encrypted))),
  );
  return btoa(String.fromCharCode.apply(null, encryptedArray)); // iv + 密文 整体 base64
}
```

> 加密规格：AES-GCM / 256 位 / 随机 12 字节 IV，输出 = base64(iv + ciphertext)。需要 HTTPS 环境（`crypto.subtle` 要求 secure context）。

### 5.5 回调实现要点

```js
const captchaVerifyCallback = async (captchaVerifyParam) => {
  const data = { captchaResult: false, bizResult: true };
  if (!captchaVerifyParam) {
    // 未验证通过，提示"请先进行安全验证!"
    return data;
  }
  captchaVerifyParam = await encryptAES(captchaVerifyParam, secret);
  data.captchaResult = true;
  await sendMessageAuthCode(captchaVerifyParam); // 发送短信接口
  return data;
};
```

---

## 6. 错误处理与错误码

### 6.1 登录场景（v2 接口）错误判断

HTTP 状态非 200 时，按 `error_description` 映射提示：

| error_description | 提示文案 |
| --- | --- |
| `phone number illegal` | 请输入正确的手机号码 |
| `Please wait a moment while trying to send.` | 操作过于频繁，请稍后再试 |
| 包含 `exceed the limit` / `SMS verification code exceeds the limit` / `This request has reached api limit.` | 验证码获取已达上限，请明日再试 |
| 其他 | 展示 `errorInfo` 或 `error_description` 原文，兜底"验证码获取失败" |

### 6.2 注册/重置密码场景错误码表

HTTP 非 200 时，响应体 `{ code, errorInfo }`，按下表映射（`code: 400` 最常见）：

| code | errorInfo | 提示文案 |
| --- | --- | --- |
| 400 | `Please wait a moment while trying to send.` | 验证码在有效期内，请勿重复发送！ |
| 400 | `Image verification code error.` | 图片验证码错误，请更换验证码或重新输入！ |
| 400 | `Image code id cannot be empty.` | 请填入图片验证码！ |
| 400 | `Phone number cannot be empty.` | 获取图片验证码请填入手机号！ |
| 400 | `phone number illegal` | 手机号不合法！ |
| 400 | `Please send SMS to get mobile phone verification code.` | 请发送短信获取手机验证码！ |
| 400 | `SMS verification code error.` | 验证码错误！ |
| 17 | `unauthorized` | 未开放授权注册！ |
| 17 | `resource_limited` | 注册已达上限请开通企业版！ |

---

## 7. 前端交互规范（倒计时）

发送成功后启动 60 秒倒计时，期间按钮禁用并显示剩余秒数：

```js
const startCountDown = () => {
  isSenedAuthCode.value = true;
  let timer = setInterval(() => {
    if (authCodeNextCansendTime.value <= 60 && authCodeNextCansendTime.value > 0) {
      authCodeNextCansendTime.value--;
    } else {
      clearInterval(timer);
      authCodeNextCansendTime.value = 60;
      isSenedAuthCode.value = false;
    }
  }, 1000);
};
```

按钮文案：倒计时中显示 `{n}s后重新获取`（登录场景显示 `{n}S`），结束后恢复"获取验证码"。

---

## 8. 接入注意事项

1. **环境要求**：AES 加密依赖 `crypto.subtle`，必须 HTTPS（或 localhost）。
2. **阿里云配置申请**：`SCENE_ID`、`PREFIX`、`SECRET` 需在阿里云验证码 2.0 控制台自行申请开通，通过环境变量注入，禁止入库。
3. **手机号校验**：三个场景均使用 `/^1[3-9]\d{9}$/` 正则校验（大陆手机号）。
4. **登录场景生产/开发差异**：阿里云验证码仅在 `NODE_ENV === 'production'` 时初始化；开发环境默认走密码登录模式（可通过 localStorage 配置 `EASEIM_CUSTOM_CONFIG.loginMode = 'sms'` 强制开启短信登录）。
5. **沙箱环境**：联调时可将 baseURL 切换为 `//a1-hsb.easemob.com`。
6. **验证码倒计时**：60 秒，与"验证码在有效期内，请勿重复发送！"（重复发送报错）配合使用。
7. **登录短信链路成功后**，还需调用 `POST /inside/app/user/login/V2`（body: `{ phoneNumber, smsCode }`）换取 IM 登录 token，返回 `{ code: 200, chatUserName, token }`，再用 `token` 打开 IM SDK——短信发送本身不依赖 IM SDK。

---

## 9. 参考源码位置（webim-vue-demo）

| 模块 | 文件 |
| --- | --- |
| 接口定义 | `src/api/login.js`、`src/api/register.js` |
| 请求封装 | `src/utils/request.js` |
| 登录短信逻辑 | `src/views/Login/components/LoginInput/index.vue` |
| 注册逻辑 | `src/views/Login/components/RegisterInput/index.vue` |
| 重置密码逻辑 | `src/views/Login/components/ResetPassword/index.vue` |
| AES 加密 | `src/utils/encriptAES.js` |
| 环境变量读取 | `src/private-config/index.js` |
| 错误码映射 | `src/constant/errorCode.js` |
| 阿里云 SDK 引入 | `public/index.html` |
